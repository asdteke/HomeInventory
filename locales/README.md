# Locale Packs

These server-side locale packs are current for the public v2.6.1 release line.

The v2.6 line keeps 103 client UI packs and 103 server-side email/default-house packs aligned, including box management and the v2.6.1 scanner privacy/status strings. English and Turkish remain manually reviewed; automated parity, key-shape, artifact, and localization-quality checks cover every locale, with per-key fallback retained as a safety net.
